import { LengthPipePipe } from './length-pipe.pipe';

describe('LengthPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LengthPipePipe();
    expect(pipe).toBeTruthy();
  });
});
